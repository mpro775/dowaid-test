
# 📌 شرح مرونة قواعد البيانات غير العلائقية (NoSQL) وكيف تم تطبيقها في المشروع

## 🎯 مقدمة
في هذا المشروع تم استخدام قاعدة بيانات **MongoDB** مع **Mongoose** لتطبيق CRUD بسيط لإدارة الأصناف.  
الفكرة الأساسية كانت توضيح **مرونة قواعد البيانات غير العلائقية (NoSQL)** مقارنة بقواعد البيانات العلائقية (SQL)، وكيف يمكننا تعديل البيانات وإضافة حقول جديدة بسهولة بدون الحاجة لتغيير هيكل قاعدة البيانات.

---

# 🧱 1. الفرق بين SQL و NoSQL (بشكل مبسّط)

## SQL (علائقية):
- تعتمد على **جداول** و **أعمدة ثابتة**.
- أي تعديل في الهيكل (Schema) يتطلب تغيير الجداول — `ALTER TABLE`.
- تحتاج Migration عند إضافة أو حذف الأعمدة.

## NoSQL (Document Database):
- تعتمد على **Documents** مثل JSON.
- كل مستند يمكن أن يحتوي على حقول مختلفة.
- لا تحتاج Migrations.
- مناسبة للبيانات المتغيرة والمرنة.

---

# 🧩 2. كيفية جعل الـ Schema مرنة في Mongoose

### ✨ افتراضيًا، Mongoose يمنع الحقول غير الموجودة
لكن MongoDB نفسها تسمح بها.

لذلك قمنا بتفعيل خاصية:
```js
strict: false
```

داخل إعدادات الـ Schema:

```js
const itemSchema = new mongoose.Schema(
  {
    name: { type: String, required: true },
    description: { type: String },
    price: { type: Number, required: true },
    quantity: { type: Number, default: 0 },
    category: { type: String },
    isAvailable: { type: Boolean, default: true }
  },
  {
    timestamps: true,
    strict: false   // 👈 يسمح بحقول إضافية غير معرفة
  }
);
```

### 📌 ماذا يعني strict: false ؟

يعني:

> أي حقل يرسله المستخدم ضمن `req.body` سيتم حفظه مباشرة داخل MongoDB حتى لو لم يكن معرفًا في الـ Schema.

🔹 مثال:

```json
{
  "name": "Product X",
  "price": 20,
  "color": "red",
  "warranty": "2 years",
  "features": {
    "weight": "1kg",
    "voltage": "220V"
  }
}
```

سيتم حفظ كل هذه البيانات مباشرة داخل Document بدون مشاكل.

---

# 🛡 3. السماح بالحقول الإضافية في الـ Validation

استخدمنا express-validator للتحقق من الحقول الأساسية فقط،  
وأضفنا Middleware يسمح بالحقول الإضافية:

```js
const allowUnknownFields = (req, res, next) => {
  next(); // 👈 NoSQL مرونة – نمرر جميع الحقول
};
```

ثم استخدمناه داخل create/update:

```js
const validateCreateItem = [
  allowUnknownFields,
  body("name").notEmpty(),
  body("price").notEmpty().isFloat({ min: 0 }),
  validate
];
```

بهذا الشكل:
- يتم التحقق من الحقول الأساسية.
- وتُترك الحقول الإضافية تمر بدون رفض (مرونة NoSQL).

---

# 🧪 4. مثال توضيحي للمرونة قبل وبعد

## ❌ في SQL:
لو أردت إضافة حقل مثل "color":
- يجب تعديل الجدول.
- يجب تحديث الـ Migrations.
- قد تتأثر البيانات القديمة.

## ✅ في MongoDB + Mongoose (strict:false):
يمكن إرسال:

```json
{
  "name": "Lamp",
  "price": 15,
  "color": "yellow",
  "specs": {
    "watt": 12,
    "type": "LED"
  }
}
```

وسيتم حفظ جميع الحقول مباشرة داخل نفس الـ document:

```json
{
  "_id": "....",
  "name": "Lamp",
  "price": 15,
  "color": "yellow",
  "specs": {
    "watt": 12,
    "type": "LED"
  },
  "createdAt": "...",
  "updatedAt": "..."
}
```

بدون أي تغييرات في الـ Schema، وبدون أي عمليات Migration.

---

# 🛠 5. كيف يستفيد المشروع من هذه المرونة؟

1. يمكن إضافة مواصفات جديدة لأي صنف بدون تعديل الكود.
2. المنتجات يمكن أن تكون مختلفة في الشكل:
   - منتج كهربائي → specs.voltage
   - منتج أثاث → specs.dimensions
   - منتج إلكتروني → specs.ram, specs.storage
3. نفس الـ Collection يمكنه حفظ مستندات مختلفة تمامًا.
4. النظام يصبح قابلًا للتطوير بشكل أسرع.

---

# 🗂 6. السيناريو الذي يمكن شرحه للدكتور

> “استخدمت NoSQL لأن مشروع الأصناف يحتاج مرونة. بعض الأصناف لها حقول إضافية تختلف عن الأخرى، لذلك فعّلت strict:false في Mongoose، وأضفت Middleware في الـ validation يسمح بتمرير الحقول الإضافية. بهذه الطريقة MongoDB تحفظ أي بيانات جديدة بدون الحاجة لتعديل الهيكل، وبدون تأثير على البيانات القديمة.”

---

# 🎯 الخلاصة

- MongoDB مرنة بطبيعتها.
- Mongoose يمكن جعله مرنًا باستخدام:
  - `strict: false`
  - Middleware للسماح بالحقول الإضافية.
- المشروع الآن يدعم البيانات الديناميكية والمتغيرة بدون الحاجة لتعديل الـ schema.
- هذا هو أهم مفهوم تود إيصاله في مادة قواعد البيانات غير العلائقية.

---

# ✅ انتهى الملف  
يمكن استخدامه في الشرح أو رفعه على GitHub أو تقديمه للدكتور.
